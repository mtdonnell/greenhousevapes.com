{
  "name": "greenhousevapes-com",
  "private": true,
  "version": "1.0.0",
  "description": "Static site plus a contact-form Worker for Greenhouse Vapes.",
  "dependencies": {
    "mimetext": "^3.0.24"
  },
  "devDependencies": {
    "@babel/cli": "^8.0.4",
    "@babel/core": "^8.0.1",
    "@babel/preset-react": "^8.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "scripts": {
    "build": "npm run build:jsx && node build/prerender.mjs",
    "build:jsx": "babel components/shell.jsx -o components/shell.compiled.js && babel components/interactive.jsx -o components/interactive.compiled.js && babel components/home.jsx -o components/home.compiled.js && babel components/pages.jsx -o components/pages.compiled.js",
    "deploy": "npm run build && wrangler deploy"
  }
}