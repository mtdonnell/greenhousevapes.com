# Netlify served NO CSP on this site — this policy is new, so anything it
# blocks is a breakage the migration introduced. Every source below was traced
# to a real reference before being allowed:
#   (React is now vendored at /vendor/, so no third-party script host)
#   fonts.googleapis Fraunces / Geist / JetBrains Mono stylesheet
#   fonts.gstatic    the font files that stylesheet pulls
#   www.google.com   StoreMap() in shell.compiled.js embeds maps?q=...&output=embed
#                    in an iframe — without frame-src the store maps go blank.

/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), microphone=(), camera=(), interest-cohort=()
  Strict-Transport-Security: max-age=31536000; includeSubDomains
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data:; frame-src https://www.google.com; base-uri 'self'; form-action 'self'; frame-ancestors 'none'

# Fingerprint-free filenames, so revalidate rather than cache hard.
/components/*
  Cache-Control: public, max-age=3600, must-revalidate

/assets/*
  Cache-Control: public, max-age=31536000, immutable

/styles.css
  Cache-Control: public, max-age=3600, must-revalidate

/home.css
  Cache-Control: public, max-age=3600, must-revalidate

# Version is pinned in the filename, so this can be cached permanently.
/vendor/*
  Cache-Control: public, max-age=31536000, immutable

# HTML carries the ?v= stamps that pin each page to its bundle, so it must
# always revalidate. The versioned assets it points at can cache hard.
/
  Cache-Control: public, max-age=0, must-revalidate

/*.html
  Cache-Control: public, max-age=0, must-revalidate
